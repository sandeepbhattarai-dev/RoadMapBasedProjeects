# My Roadmap
## week-1 : Introduction to .NET Core and ASP.NET Core
- [ ] Tasks:
  - Install .NET Core SDK and Visual Studio.
  - Understand the .NET ecosystem and .NET Core vs .NET Framework.
  - Create a new ASP.NET Core MVC project.
  - Learn the structure of an ASP.NET Core project: `Program.cs`, `Startup.cs`, `appsettings.json`.
  - **Project Output:**  
    - Create a "Hello World" ASP.NET Core MVC web application and deploy it locally.


## Solution, Explanation and Implementation Details:
 - Created a new ASP.NET Core MVC project using Visual Studio.
 - Deleted all the unnecessary files and folders to keep the project clean.
 - Implemented the "Hello World" functionality in the MVC application using a controller and a view.
 - used a "p" tag in the view to display the message.
 - Used the Default HomeController and Index view to display the message.
 - Deployed the application locally for testing.